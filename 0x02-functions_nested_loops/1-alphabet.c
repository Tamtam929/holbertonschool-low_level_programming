#include "main.h"
/**
 * print_alphabet in lowercase - prints in lowercase
 * Return: 0
 */
void print_alphabet(void)
{
	char alpha;

	alpha = 'a';

	while (alpha, = 'z')
	{
		_putchar(alpha);
		alpha++;
	}
	_Putchar('\n');
}
